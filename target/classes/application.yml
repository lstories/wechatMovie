# database
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/movdatabase?serverTimezone=GMT%2b8
    username: root
    password: lstory11
  mvc:
    hiddenmethod:
      filter:
        enabled: true

server:
  port: 8081

# mybatis
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

